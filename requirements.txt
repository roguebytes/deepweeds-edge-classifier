# Install torch/torchvision for your platform from pytorch.org if you need a
# specific CUDA build; the loose pins below work for CPU/MPS out of the box.
torch>=2.1
torchvision>=0.16
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65
Pillow>=10.0
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1     # optional: only for PyTorch's newer "dynamo" ONNX exporter (we default to the legacy exporter)
